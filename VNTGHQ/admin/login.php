<?php
/**
 * admin/login.php — redirects to the unified login page with admin mode pre-selected.
 * Keeping this file so existing bookmarks / links still work.
 */
header('Location: ../login.php?mode=admin');
exit;
